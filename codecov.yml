comment:
  layout: "diff, flags, files"

flags:
  statetests:
    paths:
      - lib/qrvmone
      - test/state
      - test/statetest
  blockchaintests:
    joined: false
    paths:
      - lib/qrvmone
